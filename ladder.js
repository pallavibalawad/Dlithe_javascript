/*let ladder = {
    step: 0,
    up() {
      this.step++;
    },
    down() {
      this.step--;
    },
    showStep: function() { // shows the current step
      alert( this.step );
    }
  };
  
  ladder.up();
  ladder.down();
  ladder.showStep();ladder.up();ladder.showStep();*/

  //splice
  let color=['red', 'white','green'];
  color.splice(1,0,pink);
  console.log(color);